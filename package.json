{
  "name": "payload-better-auth",
  "description": "Monorepo for Payload CMS Better Auth plugin and adapter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean-all": "find . -type d -name node_modules -o -name dist -o -type f -name tsconfig.tsbuildinfo -o -name pnpm-lock.yaml | xargs rm -rf"
  },
  "keywords": [
    "payload-cms",
    "better-auth",
    "plugin",
    "adapter",
    "authentication",
    "monorepo",
    "payload-plugin"
  ],
  "author": "forrestdevs <luke.gannon@me.com>",
  "license": "MIT",
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp"
    ]
  }
}
